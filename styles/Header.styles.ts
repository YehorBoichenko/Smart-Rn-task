import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  header: {
    backgroundColor: '#4CAF50',
    color: 'white',
    paddingVertical: 10,
    textAlign: 'center',
  },
  title: {
    margin: 0,
    fontSize: 24,
    color: 'white',
    textAlign: 'center',
  },
});
