import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  footer: {
    display: 'flex',
    backgroundColor: '#4CAF50',
    paddingVertical: 10,
    alignItems: 'center',
  },
  footerText: {
    color: 'white',
    fontSize: 14,
    margin: 0,
  },
});
